# Cloudflare Pages (and Netlify) redirect rules — platform feature, not app code.
# The root layout lives under /[locale]/, so bare / has no page; send it to the
# default locale. 301 = permanent, so search engines index /en/ as canonical.
/ /en/ 301
