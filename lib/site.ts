// Site identity in one place — swap these when the domain is purchased.
export const SITE_NAME = 'GovSchemes India';

// Placeholder until the real domain exists; used for canonical URLs, hreflang,
// sitemap and JSON-LD from Phase 1 onward.
export const SITE_URL = 'https://govtschemes.pages.dev';
